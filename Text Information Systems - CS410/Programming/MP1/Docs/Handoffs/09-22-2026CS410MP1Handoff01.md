# 2026-09-22-cs410-mp1-env-handoff

**Handoff Mode: Implementation**
**Receiving agent job: Resume and continue**

## 1. Thread Purpose
Get Isaac Simms’s Fedora desktop ready for CS410 MP1 (Pyserini retrieval experiments) and start the official warmup: install toolchain, place course code/datasets, run stock `main.py` until it prints a score and writes `results.json`. Do not implement Tasks 1–3 unless the user asks. User paces the work; do not dump the whole assignment.

## 2. Stack & Environment
- Host: Fedora Linux desktop. User: `isaacsimms`. System Java remains OpenJDK **25** at `/usr/bin/java` and `/usr/lib/jvm/java-25-openjdk`. Leave it.
- Miniconda: `/home/isaacsimms/miniconda3` (conda 26.7.1). ToS accepted for `pkgs/main` and `pkgs/r`.
- Env: `pyserini` → `/home/isaacsimms/miniconda3/envs/pyserini`
  - Python 3.12.14
  - OpenJDK **21.0.10** from conda-forge (`21.0.10-internal-adhoc.conda.src`)
  - Maven via conda-forge
  - CPU PyTorch from `https://download.pytorch.org/whl/cpu`
  - Pyserini **2.4.0**
- Activate hook so 21 wins after `conda activate pyserini`:
  `$CONDA_PREFIX/etc/conda/activate.d/java21.sh`
  (`JAVA_HOME=$CONDA_PREFIX`, prepend `$JAVA_HOME/bin`)
- Verified: `importlib.metadata.version("pyserini")` → `2.4.0`; `from pyserini.search.lucene import LuceneSearcher` → `lucene ok`
- Editor: VS Code. Must use interpreter `~/miniconda3/envs/pyserini/bin/python`. Run button / `/usr/bin/python` is wrong (no tqdm; first crash).
- Assignment tree:
  `/home/isaacsimms/Desktop/MCS-UIUC/Text Information Systems - CS410/Programming/MP1/CS410-MP1-code/`
- Course zips (already downloaded/extracted):
  - code: https://drive.google.com/file/d/1oB4Nc4XpdAqPWEG92DVF9XGFoG4D8L8c/view
  - data: https://drive.google.com/file/d/1FCcBPYRHC1cAUAUbptIGOV5sJaMw_aXN/view

## 3A. What Was Accomplished
1. Rejected GNOME Boxes / Ubuntu VM. Work is on the Fedora host.
2. Installed Miniconda; created env `pyserini` (Python 3.12).
3. Unblocked `CondaToSNonInteractiveError` with `conda tos accept` on both default channels.
4. Installed `openjdk=21` + `maven` from conda-forge (first fetch failed on CDN; retry worked).
5. Persisted Java 21 across deactivate/activate.
6. Installed CPU torch + `pyserini==2.4.0`. Package path is the env `site-packages`, not a local shadow file.
7. Placed course materials under the MP1 Desktop folder, not under `miniconda3`. Deleted `__MACOSX` from the Mac-packed zips.
8. Fixed starter import for Pyserini 2.4.0 in `main.py`:
   `from pyserini.index.lucene import LuceneIndexReader as IndexReader`
9. Diagnosed default-run failure: `FileNotFoundError: data/apnews/apnews.dat` because datasets sit at `CS410-MP1-code/CS410-MP1-data/{apnews,cranfield,new_faculty}` instead of `data/`.

## 4A. Current State
- Toolchain: good.
- `main.py` import: patched.
- Dataset directory: **not yet renamed**. Last command that mattered is still the missing `data/apnews/apnews.dat`.
- Warmup: **not green**. No printed score, no confirmed `results.json`.
- VS Code interpreter: user switched once; do not trust the play button until a `(pyserini)` terminal run succeeds.
- Graded tasks: not started.

## 5. Key Decisions & Rationale

| Decision | Rationale |
|----------|-----------|
| Fedora + Miniconda, no VM | User abandoned Boxes; isolation comes from the env, not a guest OS |
| Keep system Java 25 | Other software can use it; only the `pyserini` env must see 21 |
| Pin `pyserini==2.4.0`, not `==latest` | `latest` is not a PyPI version |
| No `[optional]`, no faiss, no official MS MARCO verify | Assignment warmup is BM25 on provided corpora; course says skip their verify servers |
| Alias `LuceneIndexReader as IndexReader` rather than downgrade Pyserini | Smaller change; `LuceneSearcher` still exists |
| Code/data on Desktop MP1 path | `miniconda3` is disposable toolchain |
| User-paced slices | Explicit: do not complete the assignment unprompted |

## 6. Blockers & Open Questions
- **Blocker:** folder name. Need `CS410-MP1-code/data/apnews/apnews.dat`. Fix: from `CS410-MP1-code`, `mv CS410-MP1-data data`, then `ls data/apnews` must show `apnews.dat`.
- If `apnews.dat` is nested one more level after the rename, adjust; do not guess extra wrappers.
- Default dataset in stock `main.py` is AP News, not Cranfield. Fine for warmup. Task 1 later needs Cranfield via the `###` markers.
- Index build on AP News can take several minutes; do not treat a long silent run as a hang immediately.
- Rebuild later: delete matching dirs under `indexes/` and `processed_corpus/` (per course doc).

## 7. Next Steps (Ordered)
1. In `(pyserini)`, `cd` to `CS410-MP1-code`, `mv CS410-MP1-data data`, confirm `data/apnews/apnews.dat`.
2. `python main.py`. Success = printed score + `results.json` under the dataset used.
3. If path/API errors, fix only that failure; do not start plots.
4. After one clean default run, edit `###` in `main.py` for warmup variants if the user wants that.
5. Only when the user asks: Task 1 (Cranfield BM25 `k`/`b`, nDCG@10 + P@10 plots), then Task 2 (two other rankers), then Task 3 (second dataset).
6. Deliverable later: one zip (PDF report + `.py`). Peer review 2026-10-25.

## 8. Must-Knows for the New Thread
- Response style: no fluff, no affirmations, no emojis, do not start with “Yes.” Technical and direct. Print copy-pasteable artifacts in chat; user does not want files written only to an agent sandbox they cannot open.
- Do not generate the full assignment, report, or plots unless asked.
- Always `conda activate pyserini` before running anything. `java -version` in that shell must be 21.
- Spaces in the MCS-UIUC path: quote it.
- Course `IndexReader` name is stale vs 2.4.0; alias already applied — do not revert unless they re-extract `main.py`.
- Ubiquitous Language (Module/Interface/Implementation/Depth/Seam/Adapter/Leverage/Locality) applies when those terms come up; this thread did not use them in the code.

## 9. Relevant Artifacts
- Env: `/home/isaacsimms/miniconda3/envs/pyserini`
- Activate hook: `.../envs/pyserini/etc/conda/activate.d/java21.sh`
- Work dir: `.../Programming/MP1/CS410-MP1-code/main.py`
- Datasets (wrong name until rename): `.../CS410-MP1-code/CS410-MP1-data/{apnews,cranfield,new_faculty}`
- Install reference: https://github.com/castorini/pyserini/blob/master/docs/installation.md
- Assignment Google Doc (user-provided): https://docs.google.com/document/d/1-GCSskRoyvmEjrtSTWEuVlyWBX-5vs9oxHJifly7gIc/edit
- Recap of this thread (printed 2026-09-22, not saved to sandbox)