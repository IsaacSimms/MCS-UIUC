# MapReduce Overview
**Overall paradigm**

## What is MapReduce?
*the term MapReduce has been taken from functional languages*

"Map" is a meta function that applies the function behavior to a list of inputs. Those inputs can be thought of as records
Each record is processed sequentially and independently
